import { FormatStatusPipe } from './format-status-pipe';

xdescribe('FormatStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
